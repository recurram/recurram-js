[package]
name = "gowe-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
gowe = { git = "https://github.com/gowe-team/gowe-rust.git" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
